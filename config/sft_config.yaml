model_name: Qwen/Qwen2.5-1.5B-Instruct
output_dir: outputs/sft
max_seq_length: 2048
load_in_4bit: true
seed: 42
lora:
  r: 16
  alpha: 32
  dropout: 0.0
  target_modules:
  - q_proj
  - k_proj
  - v_proj
  - o_proj
  - gate_proj
  - up_proj
  - down_proj
train:
  per_device_train_batch_size: 2
  gradient_accumulation_steps: 8
  warmup_steps: 75
  max_steps: 1500
  learning_rate: 0.0002
  logging_steps: 10
  save_steps: 100
  eval_steps: 100
  weight_decay: 0.01
  lr_scheduler_type: cosine
  optim: paged_adamw_8bit
  fp16: false
  bf16: true
  dataloader_num_workers: 4
  dataloader_pin_memory: true
  packing: true
dataset_path: data/processed/sft_train_hq_30k.json
