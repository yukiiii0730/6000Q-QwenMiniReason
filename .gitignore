# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# 训练输出（体积大，不上传）
outputs/
colab/
gpu/
*.bin
*.safetensors
*.pt
*.pth
*.tar.gz

# 数据（原始数据不上传）
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

# 评估结果图表
eval/figures/
eval/*.json
eval/*.csv

# 环境
.env
*.env
venv/
.venv/

# macOS
.DS_Store

# Jupyter checkpoints
.ipynb_checkpoints/

# 运行流水日志（不上传）
logs/runs/
logs/*.log
logs/*.csv
logs/latest
# 评测结果 JSON（本地或流水线生成，不上传）
logs/*.json

# 本地打包产物
local_artifacts.zip
*.zip

# Unsloth 编译缓存
unsloth_compiled_cache/
